// SPDX-License-Identifier: MIT
// This file was autogenerated by scripts/codegen/gen_kernels.py; DO NOT EDIT.
// Neural dispatch: softmax, paged attention, normalization, rope

#include "marmot/device.h"
#include "marmot/error.h"
#include "marmot/ops/paged_attention.h"
#include "marmot/tensor.h"

#include <stdint.h>
#include <stdlib.h>

#include "cpu_backend_internal.h"
#include "graph/kernel_dispatch_args.gen.h"

// RoPE dispatch helper - uses positions tensor if provided, otherwise generates from n_past
static marmot_error_t marmot_cpu_rope_dispatch(const void *device_ctx, const marmot_kernel_args_rope_t *unpacked) {
    if (unpacked == nullptr || unpacked->input == nullptr || unpacked->output == nullptr || unpacked->ctx == nullptr ||
        unpacked->rope_params == nullptr) {
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }

    marmot_rope_params_t params = *unpacked->rope_params;

    // If positions tensor is provided, use it directly
    if (params.positions != nullptr) {
        return cpu_rope(device_ctx, unpacked->input, &params, unpacked->output);
    }

    // Otherwise generate positions from n_past using context scratch buffer
    const size_t seq_len =
        unpacked->input->shape.ndim >= 2 ? unpacked->input->shape.shape[unpacked->input->shape.ndim - 2] : 1;

    cpu_context_t *ctx = (cpu_context_t *)device_ctx;
    int32_t *positions_data = cpu_get_rope_positions_scratch(ctx, seq_len);
    if (positions_data == nullptr) {
        marmot_set_error(MARMOT_ERROR_OUT_OF_MEMORY, "Failed to get positions scratch buffer for RoPE");
        return MARMOT_ERROR_OUT_OF_MEMORY;
    }
    for (size_t i = 0; i < seq_len; ++i) {
        positions_data[i] = (int32_t)(unpacked->n_past + i);
    }
    size_t shape[1] = {seq_len};
    marmot_tensor_t *positions = marmot_tensor_create(unpacked->ctx, shape, 1, MARMOT_DTYPE_INT32);
    if (positions == nullptr) {
        marmot_set_error(MARMOT_ERROR_OUT_OF_MEMORY, "Failed to create positions tensor for RoPE");
        return MARMOT_ERROR_OUT_OF_MEMORY;
    }
    marmot_error_t err =
        marmot_tensor_copy_from_host_buffer(unpacked->ctx, positions, positions_data, seq_len * sizeof(int32_t));
    // Note: positions_data is from scratch buffer, don't free it
    if (err != MARMOT_SUCCESS) {
        marmot_tensor_destroy(positions);
        return err;
    }
    params.positions = positions;
    err = cpu_rope(device_ctx, unpacked->input, &params, unpacked->output);
    marmot_tensor_destroy(positions);
    return err;
}

// ============================================================================
// Softmax Implementations
// ============================================================================
// softmax_f16_scalar: Softmax
marmot_error_t marmot_cpu_softmax_f16_scalar_exec(const void *device_ctx, const marmot_kernel_args_softmax_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_softmax_desc_t desc = {
        .x = args->input,
        .out = args->output,
        .axis = args->axis,
    };
    return cpu_softmax_impl(device_ctx, &desc);
}

// softmax_f32_scalar: Softmax
marmot_error_t marmot_cpu_softmax_f32_scalar_exec(const void *device_ctx, const marmot_kernel_args_softmax_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_softmax_desc_t desc = {
        .x = args->input,
        .out = args->output,
        .axis = args->axis,
    };
    return cpu_softmax_impl(device_ctx, &desc);
}

// softmax_bf16_scalar: Softmax
marmot_error_t marmot_cpu_softmax_bf16_scalar_exec(const void *device_ctx, const marmot_kernel_args_softmax_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_softmax_desc_t desc = {
        .x = args->input,
        .out = args->output,
        .axis = args->axis,
    };
    return cpu_softmax_impl(device_ctx, &desc);
}

// softmax_fp8_e4m3_scalar: Softmax
#if MARMOT_ENABLE_FP8
marmot_error_t
marmot_cpu_softmax_fp8_e4m3_scalar_exec(const void *device_ctx, const marmot_kernel_args_softmax_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_softmax_desc_t desc = {
        .x = args->input,
        .out = args->output,
        .axis = args->axis,
    };
    return cpu_softmax_impl(device_ctx, &desc);
}
#endif

// softmax_fp8_e5m2_scalar: Softmax
#if MARMOT_ENABLE_FP8
marmot_error_t
marmot_cpu_softmax_fp8_e5m2_scalar_exec(const void *device_ctx, const marmot_kernel_args_softmax_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_softmax_desc_t desc = {
        .x = args->input,
        .out = args->output,
        .axis = args->axis,
    };
    return cpu_softmax_impl(device_ctx, &desc);
}
#endif

// softmax_f64_scalar: Softmax
marmot_error_t marmot_cpu_softmax_f64_scalar_exec(const void *device_ctx, const marmot_kernel_args_softmax_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_softmax_desc_t desc = {
        .x = args->input,
        .out = args->output,
        .axis = args->axis,
    };
    return cpu_softmax_impl(device_ctx, &desc);
}

// ============================================================================
// TopK Implementations
// ============================================================================
// topk_f32_scalar: TopK
marmot_error_t marmot_cpu_topk_f32_scalar_exec(const void *device_ctx, const marmot_kernel_args_topk_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_topk_desc_t desc = {
        .x = args->input,
        .values_out = args->values_out,
        .indices_out = args->indices_out,
        .axis = args->axis,
        .k = args->k,
    };
    return cpu_topk_impl(device_ctx, &desc);
}

// topk_f16_scalar: TopK
marmot_error_t marmot_cpu_topk_f16_scalar_exec(const void *device_ctx, const marmot_kernel_args_topk_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_topk_desc_t desc = {
        .x = args->input,
        .values_out = args->values_out,
        .indices_out = args->indices_out,
        .axis = args->axis,
        .k = args->k,
    };
    return cpu_topk_impl(device_ctx, &desc);
}

// ============================================================================
// MoE Expert Implementations
// ============================================================================
// moe_experts_f32_scalar: MoE experts
marmot_error_t
marmot_cpu_moe_experts_f32_scalar_exec(const void *device_ctx, const marmot_kernel_args_moe_experts_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_moe_experts_desc_t desc = {
        .hidden_states = args->hidden_states,
        .gate_exps = args->gate_exps,
        .up_exps = args->up_exps,
        .down_exps = args->down_exps,
        .topk_ids = args->topk_ids,
        .topk_weights = args->topk_weights,
        .out = args->out,
        .ffn_type = args->ffn_type,
        .weights_scale = args->weights_scale,
        .router_weight_policy = args->router_weight_policy,
    };
    return cpu_moe_experts_impl(device_ctx, &desc);
}

// moe_experts_f16_scalar: MoE experts
marmot_error_t
marmot_cpu_moe_experts_f16_scalar_exec(const void *device_ctx, const marmot_kernel_args_moe_experts_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_moe_experts_desc_t desc = {
        .hidden_states = args->hidden_states,
        .gate_exps = args->gate_exps,
        .up_exps = args->up_exps,
        .down_exps = args->down_exps,
        .topk_ids = args->topk_ids,
        .topk_weights = args->topk_weights,
        .out = args->out,
        .ffn_type = args->ffn_type,
        .weights_scale = args->weights_scale,
        .router_weight_policy = args->router_weight_policy,
    };
    return cpu_moe_experts_impl(device_ctx, &desc);
}

// moe_experts_q4_0_scalar: MoE experts
marmot_error_t
marmot_cpu_moe_experts_q4_0_scalar_exec(const void *device_ctx, const marmot_kernel_args_moe_experts_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_moe_experts_desc_t desc = {
        .hidden_states = args->hidden_states,
        .gate_exps = args->gate_exps,
        .up_exps = args->up_exps,
        .down_exps = args->down_exps,
        .topk_ids = args->topk_ids,
        .topk_weights = args->topk_weights,
        .out = args->out,
        .ffn_type = args->ffn_type,
        .weights_scale = args->weights_scale,
        .router_weight_policy = args->router_weight_policy,
    };
    return cpu_moe_experts_impl(device_ctx, &desc);
}

// moe_experts_q4_1_scalar: MoE experts
marmot_error_t
marmot_cpu_moe_experts_q4_1_scalar_exec(const void *device_ctx, const marmot_kernel_args_moe_experts_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_moe_experts_desc_t desc = {
        .hidden_states = args->hidden_states,
        .gate_exps = args->gate_exps,
        .up_exps = args->up_exps,
        .down_exps = args->down_exps,
        .topk_ids = args->topk_ids,
        .topk_weights = args->topk_weights,
        .out = args->out,
        .ffn_type = args->ffn_type,
        .weights_scale = args->weights_scale,
        .router_weight_policy = args->router_weight_policy,
    };
    return cpu_moe_experts_impl(device_ctx, &desc);
}

// moe_experts_q5_0_scalar: MoE experts
marmot_error_t
marmot_cpu_moe_experts_q5_0_scalar_exec(const void *device_ctx, const marmot_kernel_args_moe_experts_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_moe_experts_desc_t desc = {
        .hidden_states = args->hidden_states,
        .gate_exps = args->gate_exps,
        .up_exps = args->up_exps,
        .down_exps = args->down_exps,
        .topk_ids = args->topk_ids,
        .topk_weights = args->topk_weights,
        .out = args->out,
        .ffn_type = args->ffn_type,
        .weights_scale = args->weights_scale,
        .router_weight_policy = args->router_weight_policy,
    };
    return cpu_moe_experts_impl(device_ctx, &desc);
}

// moe_experts_q5_1_scalar: MoE experts
marmot_error_t
marmot_cpu_moe_experts_q5_1_scalar_exec(const void *device_ctx, const marmot_kernel_args_moe_experts_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_moe_experts_desc_t desc = {
        .hidden_states = args->hidden_states,
        .gate_exps = args->gate_exps,
        .up_exps = args->up_exps,
        .down_exps = args->down_exps,
        .topk_ids = args->topk_ids,
        .topk_weights = args->topk_weights,
        .out = args->out,
        .ffn_type = args->ffn_type,
        .weights_scale = args->weights_scale,
        .router_weight_policy = args->router_weight_policy,
    };
    return cpu_moe_experts_impl(device_ctx, &desc);
}

// moe_experts_q8_0_scalar: MoE experts
marmot_error_t
marmot_cpu_moe_experts_q8_0_scalar_exec(const void *device_ctx, const marmot_kernel_args_moe_experts_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_moe_experts_desc_t desc = {
        .hidden_states = args->hidden_states,
        .gate_exps = args->gate_exps,
        .up_exps = args->up_exps,
        .down_exps = args->down_exps,
        .topk_ids = args->topk_ids,
        .topk_weights = args->topk_weights,
        .out = args->out,
        .ffn_type = args->ffn_type,
        .weights_scale = args->weights_scale,
        .router_weight_policy = args->router_weight_policy,
    };
    return cpu_moe_experts_impl(device_ctx, &desc);
}

// moe_experts_q8_1_scalar: MoE experts
marmot_error_t
marmot_cpu_moe_experts_q8_1_scalar_exec(const void *device_ctx, const marmot_kernel_args_moe_experts_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_moe_experts_desc_t desc = {
        .hidden_states = args->hidden_states,
        .gate_exps = args->gate_exps,
        .up_exps = args->up_exps,
        .down_exps = args->down_exps,
        .topk_ids = args->topk_ids,
        .topk_weights = args->topk_weights,
        .out = args->out,
        .ffn_type = args->ffn_type,
        .weights_scale = args->weights_scale,
        .router_weight_policy = args->router_weight_policy,
    };
    return cpu_moe_experts_impl(device_ctx, &desc);
}

// moe_experts_q2_k_scalar: MoE experts
marmot_error_t
marmot_cpu_moe_experts_q2_k_scalar_exec(const void *device_ctx, const marmot_kernel_args_moe_experts_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_moe_experts_desc_t desc = {
        .hidden_states = args->hidden_states,
        .gate_exps = args->gate_exps,
        .up_exps = args->up_exps,
        .down_exps = args->down_exps,
        .topk_ids = args->topk_ids,
        .topk_weights = args->topk_weights,
        .out = args->out,
        .ffn_type = args->ffn_type,
        .weights_scale = args->weights_scale,
        .router_weight_policy = args->router_weight_policy,
    };
    return cpu_moe_experts_impl(device_ctx, &desc);
}

// moe_experts_q3_k_scalar: MoE experts
marmot_error_t
marmot_cpu_moe_experts_q3_k_scalar_exec(const void *device_ctx, const marmot_kernel_args_moe_experts_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_moe_experts_desc_t desc = {
        .hidden_states = args->hidden_states,
        .gate_exps = args->gate_exps,
        .up_exps = args->up_exps,
        .down_exps = args->down_exps,
        .topk_ids = args->topk_ids,
        .topk_weights = args->topk_weights,
        .out = args->out,
        .ffn_type = args->ffn_type,
        .weights_scale = args->weights_scale,
        .router_weight_policy = args->router_weight_policy,
    };
    return cpu_moe_experts_impl(device_ctx, &desc);
}

// moe_experts_q4_k_scalar: MoE experts
marmot_error_t
marmot_cpu_moe_experts_q4_k_scalar_exec(const void *device_ctx, const marmot_kernel_args_moe_experts_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_moe_experts_desc_t desc = {
        .hidden_states = args->hidden_states,
        .gate_exps = args->gate_exps,
        .up_exps = args->up_exps,
        .down_exps = args->down_exps,
        .topk_ids = args->topk_ids,
        .topk_weights = args->topk_weights,
        .out = args->out,
        .ffn_type = args->ffn_type,
        .weights_scale = args->weights_scale,
        .router_weight_policy = args->router_weight_policy,
    };
    return cpu_moe_experts_impl(device_ctx, &desc);
}

// moe_experts_q5_k_scalar: MoE experts
marmot_error_t
marmot_cpu_moe_experts_q5_k_scalar_exec(const void *device_ctx, const marmot_kernel_args_moe_experts_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_moe_experts_desc_t desc = {
        .hidden_states = args->hidden_states,
        .gate_exps = args->gate_exps,
        .up_exps = args->up_exps,
        .down_exps = args->down_exps,
        .topk_ids = args->topk_ids,
        .topk_weights = args->topk_weights,
        .out = args->out,
        .ffn_type = args->ffn_type,
        .weights_scale = args->weights_scale,
        .router_weight_policy = args->router_weight_policy,
    };
    return cpu_moe_experts_impl(device_ctx, &desc);
}

// moe_experts_q6_k_scalar: MoE experts
marmot_error_t
marmot_cpu_moe_experts_q6_k_scalar_exec(const void *device_ctx, const marmot_kernel_args_moe_experts_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_moe_experts_desc_t desc = {
        .hidden_states = args->hidden_states,
        .gate_exps = args->gate_exps,
        .up_exps = args->up_exps,
        .down_exps = args->down_exps,
        .topk_ids = args->topk_ids,
        .topk_weights = args->topk_weights,
        .out = args->out,
        .ffn_type = args->ffn_type,
        .weights_scale = args->weights_scale,
        .router_weight_policy = args->router_weight_policy,
    };
    return cpu_moe_experts_impl(device_ctx, &desc);
}

// moe_experts_q8_k_scalar: MoE experts
marmot_error_t
marmot_cpu_moe_experts_q8_k_scalar_exec(const void *device_ctx, const marmot_kernel_args_moe_experts_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_moe_experts_desc_t desc = {
        .hidden_states = args->hidden_states,
        .gate_exps = args->gate_exps,
        .up_exps = args->up_exps,
        .down_exps = args->down_exps,
        .topk_ids = args->topk_ids,
        .topk_weights = args->topk_weights,
        .out = args->out,
        .ffn_type = args->ffn_type,
        .weights_scale = args->weights_scale,
        .router_weight_policy = args->router_weight_policy,
    };
    return cpu_moe_experts_impl(device_ctx, &desc);
}

// moe_experts_q4_0_f16_scalar: MoE experts
marmot_error_t
marmot_cpu_moe_experts_q4_0_f16_scalar_exec(const void *device_ctx, const marmot_kernel_args_moe_experts_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_moe_experts_desc_t desc = {
        .hidden_states = args->hidden_states,
        .gate_exps = args->gate_exps,
        .up_exps = args->up_exps,
        .down_exps = args->down_exps,
        .topk_ids = args->topk_ids,
        .topk_weights = args->topk_weights,
        .out = args->out,
        .ffn_type = args->ffn_type,
        .weights_scale = args->weights_scale,
        .router_weight_policy = args->router_weight_policy,
    };
    return cpu_moe_experts_impl(device_ctx, &desc);
}

// moe_experts_q4_1_f16_scalar: MoE experts
marmot_error_t
marmot_cpu_moe_experts_q4_1_f16_scalar_exec(const void *device_ctx, const marmot_kernel_args_moe_experts_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_moe_experts_desc_t desc = {
        .hidden_states = args->hidden_states,
        .gate_exps = args->gate_exps,
        .up_exps = args->up_exps,
        .down_exps = args->down_exps,
        .topk_ids = args->topk_ids,
        .topk_weights = args->topk_weights,
        .out = args->out,
        .ffn_type = args->ffn_type,
        .weights_scale = args->weights_scale,
        .router_weight_policy = args->router_weight_policy,
    };
    return cpu_moe_experts_impl(device_ctx, &desc);
}

// moe_experts_q5_0_f16_scalar: MoE experts
marmot_error_t
marmot_cpu_moe_experts_q5_0_f16_scalar_exec(const void *device_ctx, const marmot_kernel_args_moe_experts_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_moe_experts_desc_t desc = {
        .hidden_states = args->hidden_states,
        .gate_exps = args->gate_exps,
        .up_exps = args->up_exps,
        .down_exps = args->down_exps,
        .topk_ids = args->topk_ids,
        .topk_weights = args->topk_weights,
        .out = args->out,
        .ffn_type = args->ffn_type,
        .weights_scale = args->weights_scale,
        .router_weight_policy = args->router_weight_policy,
    };
    return cpu_moe_experts_impl(device_ctx, &desc);
}

// moe_experts_q5_1_f16_scalar: MoE experts
marmot_error_t
marmot_cpu_moe_experts_q5_1_f16_scalar_exec(const void *device_ctx, const marmot_kernel_args_moe_experts_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_moe_experts_desc_t desc = {
        .hidden_states = args->hidden_states,
        .gate_exps = args->gate_exps,
        .up_exps = args->up_exps,
        .down_exps = args->down_exps,
        .topk_ids = args->topk_ids,
        .topk_weights = args->topk_weights,
        .out = args->out,
        .ffn_type = args->ffn_type,
        .weights_scale = args->weights_scale,
        .router_weight_policy = args->router_weight_policy,
    };
    return cpu_moe_experts_impl(device_ctx, &desc);
}

// moe_experts_q8_0_f16_scalar: MoE experts
marmot_error_t
marmot_cpu_moe_experts_q8_0_f16_scalar_exec(const void *device_ctx, const marmot_kernel_args_moe_experts_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_moe_experts_desc_t desc = {
        .hidden_states = args->hidden_states,
        .gate_exps = args->gate_exps,
        .up_exps = args->up_exps,
        .down_exps = args->down_exps,
        .topk_ids = args->topk_ids,
        .topk_weights = args->topk_weights,
        .out = args->out,
        .ffn_type = args->ffn_type,
        .weights_scale = args->weights_scale,
        .router_weight_policy = args->router_weight_policy,
    };
    return cpu_moe_experts_impl(device_ctx, &desc);
}

// moe_experts_q8_1_f16_scalar: MoE experts
marmot_error_t
marmot_cpu_moe_experts_q8_1_f16_scalar_exec(const void *device_ctx, const marmot_kernel_args_moe_experts_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_moe_experts_desc_t desc = {
        .hidden_states = args->hidden_states,
        .gate_exps = args->gate_exps,
        .up_exps = args->up_exps,
        .down_exps = args->down_exps,
        .topk_ids = args->topk_ids,
        .topk_weights = args->topk_weights,
        .out = args->out,
        .ffn_type = args->ffn_type,
        .weights_scale = args->weights_scale,
        .router_weight_policy = args->router_weight_policy,
    };
    return cpu_moe_experts_impl(device_ctx, &desc);
}

// moe_experts_q2_k_f16_scalar: MoE experts
marmot_error_t
marmot_cpu_moe_experts_q2_k_f16_scalar_exec(const void *device_ctx, const marmot_kernel_args_moe_experts_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_moe_experts_desc_t desc = {
        .hidden_states = args->hidden_states,
        .gate_exps = args->gate_exps,
        .up_exps = args->up_exps,
        .down_exps = args->down_exps,
        .topk_ids = args->topk_ids,
        .topk_weights = args->topk_weights,
        .out = args->out,
        .ffn_type = args->ffn_type,
        .weights_scale = args->weights_scale,
        .router_weight_policy = args->router_weight_policy,
    };
    return cpu_moe_experts_impl(device_ctx, &desc);
}

// moe_experts_q3_k_f16_scalar: MoE experts
marmot_error_t
marmot_cpu_moe_experts_q3_k_f16_scalar_exec(const void *device_ctx, const marmot_kernel_args_moe_experts_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_moe_experts_desc_t desc = {
        .hidden_states = args->hidden_states,
        .gate_exps = args->gate_exps,
        .up_exps = args->up_exps,
        .down_exps = args->down_exps,
        .topk_ids = args->topk_ids,
        .topk_weights = args->topk_weights,
        .out = args->out,
        .ffn_type = args->ffn_type,
        .weights_scale = args->weights_scale,
        .router_weight_policy = args->router_weight_policy,
    };
    return cpu_moe_experts_impl(device_ctx, &desc);
}

// moe_experts_q4_k_f16_scalar: MoE experts
marmot_error_t
marmot_cpu_moe_experts_q4_k_f16_scalar_exec(const void *device_ctx, const marmot_kernel_args_moe_experts_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_moe_experts_desc_t desc = {
        .hidden_states = args->hidden_states,
        .gate_exps = args->gate_exps,
        .up_exps = args->up_exps,
        .down_exps = args->down_exps,
        .topk_ids = args->topk_ids,
        .topk_weights = args->topk_weights,
        .out = args->out,
        .ffn_type = args->ffn_type,
        .weights_scale = args->weights_scale,
        .router_weight_policy = args->router_weight_policy,
    };
    return cpu_moe_experts_impl(device_ctx, &desc);
}

// moe_experts_q5_k_f16_scalar: MoE experts
marmot_error_t
marmot_cpu_moe_experts_q5_k_f16_scalar_exec(const void *device_ctx, const marmot_kernel_args_moe_experts_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_moe_experts_desc_t desc = {
        .hidden_states = args->hidden_states,
        .gate_exps = args->gate_exps,
        .up_exps = args->up_exps,
        .down_exps = args->down_exps,
        .topk_ids = args->topk_ids,
        .topk_weights = args->topk_weights,
        .out = args->out,
        .ffn_type = args->ffn_type,
        .weights_scale = args->weights_scale,
        .router_weight_policy = args->router_weight_policy,
    };
    return cpu_moe_experts_impl(device_ctx, &desc);
}

// moe_experts_q6_k_f16_scalar: MoE experts
marmot_error_t
marmot_cpu_moe_experts_q6_k_f16_scalar_exec(const void *device_ctx, const marmot_kernel_args_moe_experts_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_moe_experts_desc_t desc = {
        .hidden_states = args->hidden_states,
        .gate_exps = args->gate_exps,
        .up_exps = args->up_exps,
        .down_exps = args->down_exps,
        .topk_ids = args->topk_ids,
        .topk_weights = args->topk_weights,
        .out = args->out,
        .ffn_type = args->ffn_type,
        .weights_scale = args->weights_scale,
        .router_weight_policy = args->router_weight_policy,
    };
    return cpu_moe_experts_impl(device_ctx, &desc);
}

// moe_experts_q8_k_f16_scalar: MoE experts
marmot_error_t
marmot_cpu_moe_experts_q8_k_f16_scalar_exec(const void *device_ctx, const marmot_kernel_args_moe_experts_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_moe_experts_desc_t desc = {
        .hidden_states = args->hidden_states,
        .gate_exps = args->gate_exps,
        .up_exps = args->up_exps,
        .down_exps = args->down_exps,
        .topk_ids = args->topk_ids,
        .topk_weights = args->topk_weights,
        .out = args->out,
        .ffn_type = args->ffn_type,
        .weights_scale = args->weights_scale,
        .router_weight_policy = args->router_weight_policy,
    };
    return cpu_moe_experts_impl(device_ctx, &desc);
}

// ============================================================================
// Paged Attention Implementations
// ============================================================================
// paged_attention_f16_kv_f16_accelerate: paged attention
#if MARMOT_ENABLE_ACCELERATE
marmot_error_t marmot_cpu_paged_attention_f16_kv_f16_accelerate_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif

// paged_attention_f16_kv_f16_neon: paged attention
#if MARMOT_ENABLE_NEON
marmot_error_t marmot_cpu_paged_attention_f16_kv_f16_neon_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif

// paged_attention_f16_kv_f16_avx2: paged attention
#if MARMOT_ENABLE_AVX2
marmot_error_t marmot_cpu_paged_attention_f16_kv_f16_avx2_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif

// paged_attention_f16_kv_f16_scalar: paged attention
marmot_error_t marmot_cpu_paged_attention_f16_kv_f16_scalar_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}

// paged_attention_f16_kv_f32_accelerate: paged attention
#if MARMOT_ENABLE_ACCELERATE
marmot_error_t marmot_cpu_paged_attention_f16_kv_f32_accelerate_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif

// paged_attention_f16_kv_f32_neon: paged attention
#if MARMOT_ENABLE_NEON
marmot_error_t marmot_cpu_paged_attention_f16_kv_f32_neon_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif

// paged_attention_f16_kv_f32_avx2: paged attention
#if MARMOT_ENABLE_AVX2
marmot_error_t marmot_cpu_paged_attention_f16_kv_f32_avx2_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif

// paged_attention_f16_kv_f32_scalar: paged attention
marmot_error_t marmot_cpu_paged_attention_f16_kv_f32_scalar_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}

// paged_attention_f16_kv_bf16_accelerate: paged attention
#if MARMOT_ENABLE_ACCELERATE
marmot_error_t marmot_cpu_paged_attention_f16_kv_bf16_accelerate_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif

// paged_attention_f16_kv_bf16_neon: paged attention
#if MARMOT_ENABLE_NEON
marmot_error_t marmot_cpu_paged_attention_f16_kv_bf16_neon_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif

// paged_attention_f16_kv_bf16_avx2: paged attention
#if MARMOT_ENABLE_AVX2
marmot_error_t marmot_cpu_paged_attention_f16_kv_bf16_avx2_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif

// paged_attention_f16_kv_bf16_scalar: paged attention
marmot_error_t marmot_cpu_paged_attention_f16_kv_bf16_scalar_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}

// paged_attention_f16_kv_fp8_e4m3_accelerate: paged attention
#if MARMOT_ENABLE_ACCELERATE
#if MARMOT_ENABLE_FP8
marmot_error_t marmot_cpu_paged_attention_f16_kv_fp8_e4m3_accelerate_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif
#endif

// paged_attention_f16_kv_fp8_e4m3_neon: paged attention
#if MARMOT_ENABLE_NEON
#if MARMOT_ENABLE_FP8
marmot_error_t marmot_cpu_paged_attention_f16_kv_fp8_e4m3_neon_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif
#endif

// paged_attention_f16_kv_fp8_e4m3_avx2: paged attention
#if MARMOT_ENABLE_AVX2
#if MARMOT_ENABLE_FP8
marmot_error_t marmot_cpu_paged_attention_f16_kv_fp8_e4m3_avx2_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif
#endif

// paged_attention_f16_kv_fp8_e4m3_scalar: paged attention
#if MARMOT_ENABLE_FP8
marmot_error_t marmot_cpu_paged_attention_f16_kv_fp8_e4m3_scalar_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif

// paged_attention_f32_kv_f16_accelerate: paged attention
#if MARMOT_ENABLE_ACCELERATE
marmot_error_t marmot_cpu_paged_attention_f32_kv_f16_accelerate_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif

// paged_attention_f32_kv_f16_neon: paged attention
#if MARMOT_ENABLE_NEON
marmot_error_t marmot_cpu_paged_attention_f32_kv_f16_neon_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif

// paged_attention_f32_kv_f16_avx2: paged attention
#if MARMOT_ENABLE_AVX2
marmot_error_t marmot_cpu_paged_attention_f32_kv_f16_avx2_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif

// paged_attention_f32_kv_f16_scalar: paged attention
marmot_error_t marmot_cpu_paged_attention_f32_kv_f16_scalar_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}

// paged_attention_f32_kv_f32_accelerate: paged attention
#if MARMOT_ENABLE_ACCELERATE
marmot_error_t marmot_cpu_paged_attention_f32_kv_f32_accelerate_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif

// paged_attention_f32_kv_f32_neon: paged attention
#if MARMOT_ENABLE_NEON
marmot_error_t marmot_cpu_paged_attention_f32_kv_f32_neon_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif

// paged_attention_f32_kv_f32_avx2: paged attention
#if MARMOT_ENABLE_AVX2
marmot_error_t marmot_cpu_paged_attention_f32_kv_f32_avx2_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif

// paged_attention_f32_kv_f32_scalar: paged attention
marmot_error_t marmot_cpu_paged_attention_f32_kv_f32_scalar_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}

// paged_attention_f32_kv_bf16_accelerate: paged attention
#if MARMOT_ENABLE_ACCELERATE
marmot_error_t marmot_cpu_paged_attention_f32_kv_bf16_accelerate_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif

// paged_attention_f32_kv_bf16_neon: paged attention
#if MARMOT_ENABLE_NEON
marmot_error_t marmot_cpu_paged_attention_f32_kv_bf16_neon_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif

// paged_attention_f32_kv_bf16_avx2: paged attention
#if MARMOT_ENABLE_AVX2
marmot_error_t marmot_cpu_paged_attention_f32_kv_bf16_avx2_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif

// paged_attention_f32_kv_bf16_scalar: paged attention
marmot_error_t marmot_cpu_paged_attention_f32_kv_bf16_scalar_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}

// paged_attention_f32_kv_fp8_e4m3_accelerate: paged attention
#if MARMOT_ENABLE_ACCELERATE
#if MARMOT_ENABLE_FP8
marmot_error_t marmot_cpu_paged_attention_f32_kv_fp8_e4m3_accelerate_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif
#endif

// paged_attention_f32_kv_fp8_e4m3_neon: paged attention
#if MARMOT_ENABLE_NEON
#if MARMOT_ENABLE_FP8
marmot_error_t marmot_cpu_paged_attention_f32_kv_fp8_e4m3_neon_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif
#endif

// paged_attention_f32_kv_fp8_e4m3_avx2: paged attention
#if MARMOT_ENABLE_AVX2
#if MARMOT_ENABLE_FP8
marmot_error_t marmot_cpu_paged_attention_f32_kv_fp8_e4m3_avx2_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif
#endif

// paged_attention_f32_kv_fp8_e4m3_scalar: paged attention
#if MARMOT_ENABLE_FP8
marmot_error_t marmot_cpu_paged_attention_f32_kv_fp8_e4m3_scalar_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif

// paged_attention_bf16_kv_f16_accelerate: paged attention
#if MARMOT_ENABLE_ACCELERATE
marmot_error_t marmot_cpu_paged_attention_bf16_kv_f16_accelerate_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif

// paged_attention_bf16_kv_f16_neon: paged attention
#if MARMOT_ENABLE_NEON
marmot_error_t marmot_cpu_paged_attention_bf16_kv_f16_neon_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif

// paged_attention_bf16_kv_f16_avx2: paged attention
#if MARMOT_ENABLE_AVX2
marmot_error_t marmot_cpu_paged_attention_bf16_kv_f16_avx2_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif

// paged_attention_bf16_kv_f16_scalar: paged attention
marmot_error_t marmot_cpu_paged_attention_bf16_kv_f16_scalar_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}

// paged_attention_bf16_kv_f32_accelerate: paged attention
#if MARMOT_ENABLE_ACCELERATE
marmot_error_t marmot_cpu_paged_attention_bf16_kv_f32_accelerate_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif

// paged_attention_bf16_kv_f32_neon: paged attention
#if MARMOT_ENABLE_NEON
marmot_error_t marmot_cpu_paged_attention_bf16_kv_f32_neon_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif

// paged_attention_bf16_kv_f32_avx2: paged attention
#if MARMOT_ENABLE_AVX2
marmot_error_t marmot_cpu_paged_attention_bf16_kv_f32_avx2_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif

// paged_attention_bf16_kv_f32_scalar: paged attention
marmot_error_t marmot_cpu_paged_attention_bf16_kv_f32_scalar_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}

// paged_attention_bf16_kv_bf16_accelerate: paged attention
#if MARMOT_ENABLE_ACCELERATE
marmot_error_t marmot_cpu_paged_attention_bf16_kv_bf16_accelerate_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif

// paged_attention_bf16_kv_bf16_neon: paged attention
#if MARMOT_ENABLE_NEON
marmot_error_t marmot_cpu_paged_attention_bf16_kv_bf16_neon_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif

// paged_attention_bf16_kv_bf16_avx2: paged attention
#if MARMOT_ENABLE_AVX2
marmot_error_t marmot_cpu_paged_attention_bf16_kv_bf16_avx2_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif

// paged_attention_bf16_kv_bf16_scalar: paged attention
marmot_error_t marmot_cpu_paged_attention_bf16_kv_bf16_scalar_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}

// paged_attention_bf16_kv_fp8_e4m3_accelerate: paged attention
#if MARMOT_ENABLE_ACCELERATE
#if MARMOT_ENABLE_FP8
marmot_error_t marmot_cpu_paged_attention_bf16_kv_fp8_e4m3_accelerate_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif
#endif

// paged_attention_bf16_kv_fp8_e4m3_neon: paged attention
#if MARMOT_ENABLE_NEON
#if MARMOT_ENABLE_FP8
marmot_error_t marmot_cpu_paged_attention_bf16_kv_fp8_e4m3_neon_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif
#endif

// paged_attention_bf16_kv_fp8_e4m3_avx2: paged attention
#if MARMOT_ENABLE_AVX2
#if MARMOT_ENABLE_FP8
marmot_error_t marmot_cpu_paged_attention_bf16_kv_fp8_e4m3_avx2_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif
#endif

// paged_attention_bf16_kv_fp8_e4m3_scalar: paged attention
#if MARMOT_ENABLE_FP8
marmot_error_t marmot_cpu_paged_attention_bf16_kv_fp8_e4m3_scalar_exec(
    const void *device_ctx, const marmot_kernel_args_paged_attention_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_paged_attention_desc_t desc = marmot_paged_attention_desc_default();
    desc.token_count = args->token_count;
    desc.layer_idx = args->layer_idx;
    desc.num_q_heads = args->num_q_heads;
    desc.num_kv_heads = args->num_kv_heads;
    desc.head_dim = args->head_dim;
    desc.block_size = args->block_size;
    desc.scale = args->scale;
    desc.token_meta = args->token_meta;
    desc.q = args->q;
    desc.k_new = args->k_new;
    desc.v_new = args->v_new;
    desc.kv_k = args->kv_k;
    desc.kv_v = args->kv_v;
    desc.block_table = args->block_table;
    desc.out = args->out;
    marmot_paged_attention_kv_scale_ext_t scale_ext = marmot_paged_attention_kv_scale_ext_default();
    if (args->kv_k_scale != nullptr || args->kv_v_scale != nullptr) {
        scale_ext.kv_k_scale = args->kv_k_scale;
        scale_ext.kv_v_scale = args->kv_v_scale;
        desc.pnext = &scale_ext;
    }
    return cpu_paged_attention_impl(device_ctx, &desc);
}
#endif

// ============================================================================
// RMS Normalization Implementations
// ============================================================================
// rms_norm_f16_accelerate: RMS normalization
#if MARMOT_ENABLE_ACCELERATE
marmot_error_t
marmot_cpu_rms_norm_f16_accelerate_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_impl(device_ctx, &desc);
}
#endif

// rms_norm_f16_neon: RMS normalization
#if MARMOT_ENABLE_NEON
marmot_error_t marmot_cpu_rms_norm_f16_neon_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_impl(device_ctx, &desc);
}
#endif

// rms_norm_f16_avx2: RMS normalization
#if MARMOT_ENABLE_AVX2
marmot_error_t marmot_cpu_rms_norm_f16_avx2_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_impl(device_ctx, &desc);
}
#endif

// rms_norm_f16_scalar: RMS normalization
marmot_error_t marmot_cpu_rms_norm_f16_scalar_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_impl(device_ctx, &desc);
}

// rms_norm_f32_accelerate: RMS normalization
#if MARMOT_ENABLE_ACCELERATE
marmot_error_t
marmot_cpu_rms_norm_f32_accelerate_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_impl(device_ctx, &desc);
}
#endif

// rms_norm_f32_neon: RMS normalization
#if MARMOT_ENABLE_NEON
marmot_error_t marmot_cpu_rms_norm_f32_neon_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_impl(device_ctx, &desc);
}
#endif

// rms_norm_f32_avx2: RMS normalization
#if MARMOT_ENABLE_AVX2
marmot_error_t marmot_cpu_rms_norm_f32_avx2_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_impl(device_ctx, &desc);
}
#endif

// rms_norm_f32_scalar: RMS normalization
marmot_error_t marmot_cpu_rms_norm_f32_scalar_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_impl(device_ctx, &desc);
}

// rms_norm_bf16_accelerate: RMS normalization
#if MARMOT_ENABLE_ACCELERATE
marmot_error_t
marmot_cpu_rms_norm_bf16_accelerate_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_impl(device_ctx, &desc);
}
#endif

// rms_norm_bf16_neon: RMS normalization
#if MARMOT_ENABLE_NEON
marmot_error_t marmot_cpu_rms_norm_bf16_neon_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_impl(device_ctx, &desc);
}
#endif

// rms_norm_bf16_avx2: RMS normalization
#if MARMOT_ENABLE_AVX2
marmot_error_t marmot_cpu_rms_norm_bf16_avx2_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_impl(device_ctx, &desc);
}
#endif

// rms_norm_bf16_scalar: RMS normalization
marmot_error_t marmot_cpu_rms_norm_bf16_scalar_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_impl(device_ctx, &desc);
}

// rms_norm_fp8_e4m3_accelerate: RMS normalization
#if MARMOT_ENABLE_ACCELERATE
#if MARMOT_ENABLE_FP8
marmot_error_t
marmot_cpu_rms_norm_fp8_e4m3_accelerate_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_impl(device_ctx, &desc);
}
#endif
#endif

// rms_norm_fp8_e4m3_neon: RMS normalization
#if MARMOT_ENABLE_NEON
#if MARMOT_ENABLE_FP8
marmot_error_t
marmot_cpu_rms_norm_fp8_e4m3_neon_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_impl(device_ctx, &desc);
}
#endif
#endif

// rms_norm_fp8_e4m3_avx2: RMS normalization
#if MARMOT_ENABLE_AVX2
#if MARMOT_ENABLE_FP8
marmot_error_t
marmot_cpu_rms_norm_fp8_e4m3_avx2_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_impl(device_ctx, &desc);
}
#endif
#endif

// rms_norm_fp8_e4m3_scalar: RMS normalization
#if MARMOT_ENABLE_FP8
marmot_error_t
marmot_cpu_rms_norm_fp8_e4m3_scalar_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_impl(device_ctx, &desc);
}
#endif

// rms_norm_fp8_e5m2_accelerate: RMS normalization
#if MARMOT_ENABLE_ACCELERATE
#if MARMOT_ENABLE_FP8
marmot_error_t
marmot_cpu_rms_norm_fp8_e5m2_accelerate_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_impl(device_ctx, &desc);
}
#endif
#endif

// rms_norm_fp8_e5m2_neon: RMS normalization
#if MARMOT_ENABLE_NEON
#if MARMOT_ENABLE_FP8
marmot_error_t
marmot_cpu_rms_norm_fp8_e5m2_neon_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_impl(device_ctx, &desc);
}
#endif
#endif

// rms_norm_fp8_e5m2_avx2: RMS normalization
#if MARMOT_ENABLE_AVX2
#if MARMOT_ENABLE_FP8
marmot_error_t
marmot_cpu_rms_norm_fp8_e5m2_avx2_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_impl(device_ctx, &desc);
}
#endif
#endif

// rms_norm_fp8_e5m2_scalar: RMS normalization
#if MARMOT_ENABLE_FP8
marmot_error_t
marmot_cpu_rms_norm_fp8_e5m2_scalar_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_impl(device_ctx, &desc);
}
#endif

// rms_norm_f64_accelerate: RMS normalization
#if MARMOT_ENABLE_ACCELERATE
marmot_error_t
marmot_cpu_rms_norm_f64_accelerate_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_impl(device_ctx, &desc);
}
#endif

// rms_norm_f64_neon: RMS normalization
#if MARMOT_ENABLE_NEON
marmot_error_t marmot_cpu_rms_norm_f64_neon_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_impl(device_ctx, &desc);
}
#endif

// rms_norm_f64_avx2: RMS normalization
#if MARMOT_ENABLE_AVX2
marmot_error_t marmot_cpu_rms_norm_f64_avx2_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_impl(device_ctx, &desc);
}
#endif

// rms_norm_f64_scalar: RMS normalization
marmot_error_t marmot_cpu_rms_norm_f64_scalar_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_impl(device_ctx, &desc);
}

// rms_norm_gemma_f16_accelerate: RMS normalization
#if MARMOT_ENABLE_ACCELERATE
marmot_error_t
marmot_cpu_rms_norm_gemma_f16_accelerate_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_gemma_impl(device_ctx, &desc);
}
#endif

// rms_norm_gemma_f16_neon: RMS normalization
#if MARMOT_ENABLE_NEON
marmot_error_t
marmot_cpu_rms_norm_gemma_f16_neon_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_gemma_impl(device_ctx, &desc);
}
#endif

// rms_norm_gemma_f16_avx2: RMS normalization
#if MARMOT_ENABLE_AVX2
marmot_error_t
marmot_cpu_rms_norm_gemma_f16_avx2_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_gemma_impl(device_ctx, &desc);
}
#endif

// rms_norm_gemma_f16_scalar: RMS normalization
marmot_error_t
marmot_cpu_rms_norm_gemma_f16_scalar_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_gemma_impl(device_ctx, &desc);
}

// rms_norm_gemma_f32_accelerate: RMS normalization
#if MARMOT_ENABLE_ACCELERATE
marmot_error_t
marmot_cpu_rms_norm_gemma_f32_accelerate_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_gemma_impl(device_ctx, &desc);
}
#endif

// rms_norm_gemma_f32_neon: RMS normalization
#if MARMOT_ENABLE_NEON
marmot_error_t
marmot_cpu_rms_norm_gemma_f32_neon_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_gemma_impl(device_ctx, &desc);
}
#endif

// rms_norm_gemma_f32_avx2: RMS normalization
#if MARMOT_ENABLE_AVX2
marmot_error_t
marmot_cpu_rms_norm_gemma_f32_avx2_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_gemma_impl(device_ctx, &desc);
}
#endif

// rms_norm_gemma_f32_scalar: RMS normalization
marmot_error_t
marmot_cpu_rms_norm_gemma_f32_scalar_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_gemma_impl(device_ctx, &desc);
}

// rms_norm_gemma_bf16_accelerate: RMS normalization
#if MARMOT_ENABLE_ACCELERATE
marmot_error_t
marmot_cpu_rms_norm_gemma_bf16_accelerate_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_gemma_impl(device_ctx, &desc);
}
#endif

// rms_norm_gemma_bf16_neon: RMS normalization
#if MARMOT_ENABLE_NEON
marmot_error_t
marmot_cpu_rms_norm_gemma_bf16_neon_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_gemma_impl(device_ctx, &desc);
}
#endif

// rms_norm_gemma_bf16_avx2: RMS normalization
#if MARMOT_ENABLE_AVX2
marmot_error_t
marmot_cpu_rms_norm_gemma_bf16_avx2_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_gemma_impl(device_ctx, &desc);
}
#endif

// rms_norm_gemma_bf16_scalar: RMS normalization
marmot_error_t
marmot_cpu_rms_norm_gemma_bf16_scalar_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_gemma_impl(device_ctx, &desc);
}

// rms_norm_gemma_fp8_e4m3_accelerate: RMS normalization
#if MARMOT_ENABLE_ACCELERATE
#if MARMOT_ENABLE_FP8
marmot_error_t
marmot_cpu_rms_norm_gemma_fp8_e4m3_accelerate_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_gemma_impl(device_ctx, &desc);
}
#endif
#endif

// rms_norm_gemma_fp8_e4m3_neon: RMS normalization
#if MARMOT_ENABLE_NEON
#if MARMOT_ENABLE_FP8
marmot_error_t
marmot_cpu_rms_norm_gemma_fp8_e4m3_neon_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_gemma_impl(device_ctx, &desc);
}
#endif
#endif

// rms_norm_gemma_fp8_e4m3_avx2: RMS normalization
#if MARMOT_ENABLE_AVX2
#if MARMOT_ENABLE_FP8
marmot_error_t
marmot_cpu_rms_norm_gemma_fp8_e4m3_avx2_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_gemma_impl(device_ctx, &desc);
}
#endif
#endif

// rms_norm_gemma_fp8_e4m3_scalar: RMS normalization
#if MARMOT_ENABLE_FP8
marmot_error_t
marmot_cpu_rms_norm_gemma_fp8_e4m3_scalar_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_gemma_impl(device_ctx, &desc);
}
#endif

// rms_norm_gemma_fp8_e5m2_accelerate: RMS normalization
#if MARMOT_ENABLE_ACCELERATE
#if MARMOT_ENABLE_FP8
marmot_error_t
marmot_cpu_rms_norm_gemma_fp8_e5m2_accelerate_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_gemma_impl(device_ctx, &desc);
}
#endif
#endif

// rms_norm_gemma_fp8_e5m2_neon: RMS normalization
#if MARMOT_ENABLE_NEON
#if MARMOT_ENABLE_FP8
marmot_error_t
marmot_cpu_rms_norm_gemma_fp8_e5m2_neon_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_gemma_impl(device_ctx, &desc);
}
#endif
#endif

// rms_norm_gemma_fp8_e5m2_avx2: RMS normalization
#if MARMOT_ENABLE_AVX2
#if MARMOT_ENABLE_FP8
marmot_error_t
marmot_cpu_rms_norm_gemma_fp8_e5m2_avx2_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_gemma_impl(device_ctx, &desc);
}
#endif
#endif

// rms_norm_gemma_fp8_e5m2_scalar: RMS normalization
#if MARMOT_ENABLE_FP8
marmot_error_t
marmot_cpu_rms_norm_gemma_fp8_e5m2_scalar_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_gemma_impl(device_ctx, &desc);
}
#endif

// rms_norm_gemma_f64_accelerate: RMS normalization
#if MARMOT_ENABLE_ACCELERATE
marmot_error_t
marmot_cpu_rms_norm_gemma_f64_accelerate_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_gemma_impl(device_ctx, &desc);
}
#endif

// rms_norm_gemma_f64_neon: RMS normalization
#if MARMOT_ENABLE_NEON
marmot_error_t
marmot_cpu_rms_norm_gemma_f64_neon_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_gemma_impl(device_ctx, &desc);
}
#endif

// rms_norm_gemma_f64_avx2: RMS normalization
#if MARMOT_ENABLE_AVX2
marmot_error_t
marmot_cpu_rms_norm_gemma_f64_avx2_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_gemma_impl(device_ctx, &desc);
}
#endif

// rms_norm_gemma_f64_scalar: RMS normalization
marmot_error_t
marmot_cpu_rms_norm_gemma_f64_scalar_exec(const void *device_ctx, const marmot_kernel_args_rms_norm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_rmsnorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_rmsnorm_gemma_impl(device_ctx, &desc);
}

// ============================================================================
// Layer Normalization Implementations
// ============================================================================
// layernorm_f16_accelerate: Layer normalization
#if MARMOT_ENABLE_ACCELERATE
marmot_error_t
marmot_cpu_layernorm_f16_accelerate_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif

// layernorm_f16_neon: Layer normalization
#if MARMOT_ENABLE_NEON
marmot_error_t marmot_cpu_layernorm_f16_neon_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif

// layernorm_f16_avx2: Layer normalization
#if MARMOT_ENABLE_AVX2
marmot_error_t marmot_cpu_layernorm_f16_avx2_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif

// layernorm_f16_scalar: Layer normalization
marmot_error_t
marmot_cpu_layernorm_f16_scalar_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}

// layernorm_f32_accelerate: Layer normalization
#if MARMOT_ENABLE_ACCELERATE
marmot_error_t
marmot_cpu_layernorm_f32_accelerate_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif

// layernorm_f32_neon: Layer normalization
#if MARMOT_ENABLE_NEON
marmot_error_t marmot_cpu_layernorm_f32_neon_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif

// layernorm_f32_avx2: Layer normalization
#if MARMOT_ENABLE_AVX2
marmot_error_t marmot_cpu_layernorm_f32_avx2_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif

// layernorm_f32_scalar: Layer normalization
marmot_error_t
marmot_cpu_layernorm_f32_scalar_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}

// layernorm_bf16_accelerate: Layer normalization
#if MARMOT_ENABLE_ACCELERATE
marmot_error_t
marmot_cpu_layernorm_bf16_accelerate_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif

// layernorm_bf16_neon: Layer normalization
#if MARMOT_ENABLE_NEON
marmot_error_t marmot_cpu_layernorm_bf16_neon_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif

// layernorm_bf16_avx2: Layer normalization
#if MARMOT_ENABLE_AVX2
marmot_error_t marmot_cpu_layernorm_bf16_avx2_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif

// layernorm_bf16_scalar: Layer normalization
marmot_error_t
marmot_cpu_layernorm_bf16_scalar_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}

// layernorm_fp8_e4m3_accelerate: Layer normalization
#if MARMOT_ENABLE_ACCELERATE
#if MARMOT_ENABLE_FP8
marmot_error_t
marmot_cpu_layernorm_fp8_e4m3_accelerate_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif
#endif

// layernorm_fp8_e4m3_neon: Layer normalization
#if MARMOT_ENABLE_NEON
#if MARMOT_ENABLE_FP8
marmot_error_t
marmot_cpu_layernorm_fp8_e4m3_neon_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif
#endif

// layernorm_fp8_e4m3_avx2: Layer normalization
#if MARMOT_ENABLE_AVX2
#if MARMOT_ENABLE_FP8
marmot_error_t
marmot_cpu_layernorm_fp8_e4m3_avx2_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif
#endif

// layernorm_fp8_e4m3_scalar: Layer normalization
#if MARMOT_ENABLE_FP8
marmot_error_t
marmot_cpu_layernorm_fp8_e4m3_scalar_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif

// layernorm_fp8_e5m2_accelerate: Layer normalization
#if MARMOT_ENABLE_ACCELERATE
#if MARMOT_ENABLE_FP8
marmot_error_t
marmot_cpu_layernorm_fp8_e5m2_accelerate_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif
#endif

// layernorm_fp8_e5m2_neon: Layer normalization
#if MARMOT_ENABLE_NEON
#if MARMOT_ENABLE_FP8
marmot_error_t
marmot_cpu_layernorm_fp8_e5m2_neon_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif
#endif

// layernorm_fp8_e5m2_avx2: Layer normalization
#if MARMOT_ENABLE_AVX2
#if MARMOT_ENABLE_FP8
marmot_error_t
marmot_cpu_layernorm_fp8_e5m2_avx2_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif
#endif

// layernorm_fp8_e5m2_scalar: Layer normalization
#if MARMOT_ENABLE_FP8
marmot_error_t
marmot_cpu_layernorm_fp8_e5m2_scalar_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif

// layernorm_f64_accelerate: Layer normalization
#if MARMOT_ENABLE_ACCELERATE
marmot_error_t
marmot_cpu_layernorm_f64_accelerate_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif

// layernorm_f64_neon: Layer normalization
#if MARMOT_ENABLE_NEON
marmot_error_t marmot_cpu_layernorm_f64_neon_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif

// layernorm_f64_avx2: Layer normalization
#if MARMOT_ENABLE_AVX2
marmot_error_t marmot_cpu_layernorm_f64_avx2_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif

// layernorm_f64_scalar: Layer normalization
marmot_error_t
marmot_cpu_layernorm_f64_scalar_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}

// layernorm_f16_residual_accelerate: Layer normalization
#if MARMOT_ENABLE_ACCELERATE
marmot_error_t
marmot_cpu_layernorm_f16_residual_accelerate_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif

// layernorm_f16_residual_neon: Layer normalization
#if MARMOT_ENABLE_NEON
marmot_error_t
marmot_cpu_layernorm_f16_residual_neon_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif

// layernorm_f16_residual_avx2: Layer normalization
#if MARMOT_ENABLE_AVX2
marmot_error_t
marmot_cpu_layernorm_f16_residual_avx2_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif

// layernorm_f16_residual_scalar: Layer normalization
marmot_error_t
marmot_cpu_layernorm_f16_residual_scalar_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}

// layernorm_f32_residual_accelerate: Layer normalization
#if MARMOT_ENABLE_ACCELERATE
marmot_error_t
marmot_cpu_layernorm_f32_residual_accelerate_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif

// layernorm_f32_residual_neon: Layer normalization
#if MARMOT_ENABLE_NEON
marmot_error_t
marmot_cpu_layernorm_f32_residual_neon_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif

// layernorm_f32_residual_avx2: Layer normalization
#if MARMOT_ENABLE_AVX2
marmot_error_t
marmot_cpu_layernorm_f32_residual_avx2_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif

// layernorm_f32_residual_scalar: Layer normalization
marmot_error_t
marmot_cpu_layernorm_f32_residual_scalar_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}

// layernorm_bf16_residual_accelerate: Layer normalization
#if MARMOT_ENABLE_ACCELERATE
marmot_error_t
marmot_cpu_layernorm_bf16_residual_accelerate_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif

// layernorm_bf16_residual_neon: Layer normalization
#if MARMOT_ENABLE_NEON
marmot_error_t
marmot_cpu_layernorm_bf16_residual_neon_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif

// layernorm_bf16_residual_avx2: Layer normalization
#if MARMOT_ENABLE_AVX2
marmot_error_t
marmot_cpu_layernorm_bf16_residual_avx2_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif

// layernorm_bf16_residual_scalar: Layer normalization
marmot_error_t
marmot_cpu_layernorm_bf16_residual_scalar_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}

// layernorm_fp8_e4m3_residual_accelerate: Layer normalization
#if MARMOT_ENABLE_ACCELERATE
#if MARMOT_ENABLE_FP8
marmot_error_t marmot_cpu_layernorm_fp8_e4m3_residual_accelerate_exec(
    const void *device_ctx, const marmot_kernel_args_layernorm_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif
#endif

// layernorm_fp8_e4m3_residual_neon: Layer normalization
#if MARMOT_ENABLE_NEON
#if MARMOT_ENABLE_FP8
marmot_error_t
marmot_cpu_layernorm_fp8_e4m3_residual_neon_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif
#endif

// layernorm_fp8_e4m3_residual_avx2: Layer normalization
#if MARMOT_ENABLE_AVX2
#if MARMOT_ENABLE_FP8
marmot_error_t
marmot_cpu_layernorm_fp8_e4m3_residual_avx2_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif
#endif

// layernorm_fp8_e4m3_residual_scalar: Layer normalization
#if MARMOT_ENABLE_FP8
marmot_error_t
marmot_cpu_layernorm_fp8_e4m3_residual_scalar_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif

// layernorm_fp8_e5m2_residual_accelerate: Layer normalization
#if MARMOT_ENABLE_ACCELERATE
#if MARMOT_ENABLE_FP8
marmot_error_t marmot_cpu_layernorm_fp8_e5m2_residual_accelerate_exec(
    const void *device_ctx, const marmot_kernel_args_layernorm_t *args
) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif
#endif

// layernorm_fp8_e5m2_residual_neon: Layer normalization
#if MARMOT_ENABLE_NEON
#if MARMOT_ENABLE_FP8
marmot_error_t
marmot_cpu_layernorm_fp8_e5m2_residual_neon_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif
#endif

// layernorm_fp8_e5m2_residual_avx2: Layer normalization
#if MARMOT_ENABLE_AVX2
#if MARMOT_ENABLE_FP8
marmot_error_t
marmot_cpu_layernorm_fp8_e5m2_residual_avx2_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif
#endif

// layernorm_fp8_e5m2_residual_scalar: Layer normalization
#if MARMOT_ENABLE_FP8
marmot_error_t
marmot_cpu_layernorm_fp8_e5m2_residual_scalar_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif

// layernorm_f64_residual_accelerate: Layer normalization
#if MARMOT_ENABLE_ACCELERATE
marmot_error_t
marmot_cpu_layernorm_f64_residual_accelerate_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif

// layernorm_f64_residual_neon: Layer normalization
#if MARMOT_ENABLE_NEON
marmot_error_t
marmot_cpu_layernorm_f64_residual_neon_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif

// layernorm_f64_residual_avx2: Layer normalization
#if MARMOT_ENABLE_AVX2
marmot_error_t
marmot_cpu_layernorm_f64_residual_avx2_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}
#endif

// layernorm_f64_residual_scalar: Layer normalization
marmot_error_t
marmot_cpu_layernorm_f64_residual_scalar_exec(const void *device_ctx, const marmot_kernel_args_layernorm_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_layernorm_desc_t desc = {
        .x = args->input,
        .weight = args->weight,
        .bias = args->bias,
        .residual = args->residual,
        .out = args->output,
        .eps = args->eps,
    };
    return cpu_layernorm_impl(device_ctx, &desc);
}

// ============================================================================
// RoPE Implementations
// ============================================================================
// rope_f16_scalar: RoPE operation
marmot_error_t marmot_cpu_rope_f16_scalar_exec(const void *device_ctx, const marmot_kernel_args_rope_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    return marmot_cpu_rope_dispatch(device_ctx, args);
}

// rope_f32_scalar: RoPE operation
marmot_error_t marmot_cpu_rope_f32_scalar_exec(const void *device_ctx, const marmot_kernel_args_rope_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    return marmot_cpu_rope_dispatch(device_ctx, args);
}

// rope_bf16_scalar: RoPE operation
marmot_error_t marmot_cpu_rope_bf16_scalar_exec(const void *device_ctx, const marmot_kernel_args_rope_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    return marmot_cpu_rope_dispatch(device_ctx, args);
}

// rope_fp8_e4m3_scalar: RoPE operation
#if MARMOT_ENABLE_FP8
marmot_error_t marmot_cpu_rope_fp8_e4m3_scalar_exec(const void *device_ctx, const marmot_kernel_args_rope_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    return marmot_cpu_rope_dispatch(device_ctx, args);
}
#endif

// rope_fp8_e5m2_scalar: RoPE operation
#if MARMOT_ENABLE_FP8
marmot_error_t marmot_cpu_rope_fp8_e5m2_scalar_exec(const void *device_ctx, const marmot_kernel_args_rope_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    return marmot_cpu_rope_dispatch(device_ctx, args);
}
#endif

// rope_f64_scalar: RoPE operation
marmot_error_t marmot_cpu_rope_f64_scalar_exec(const void *device_ctx, const marmot_kernel_args_rope_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    return marmot_cpu_rope_dispatch(device_ctx, args);
}

// rope_f16_neon: RoPE operation
#if MARMOT_ENABLE_NEON
marmot_error_t marmot_cpu_rope_f16_neon_exec(const void *device_ctx, const marmot_kernel_args_rope_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    return marmot_cpu_rope_dispatch(device_ctx, args);
}
#endif

// rope_f16_accelerate: RoPE operation
#if MARMOT_ENABLE_ACCELERATE
marmot_error_t marmot_cpu_rope_f16_accelerate_exec(const void *device_ctx, const marmot_kernel_args_rope_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    return marmot_cpu_rope_dispatch(device_ctx, args);
}
#endif

// rope_f32_neon: RoPE operation
#if MARMOT_ENABLE_NEON
marmot_error_t marmot_cpu_rope_f32_neon_exec(const void *device_ctx, const marmot_kernel_args_rope_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    return marmot_cpu_rope_dispatch(device_ctx, args);
}
#endif

// rope_f32_avx2: RoPE operation
#if MARMOT_ENABLE_AVX2
marmot_error_t marmot_cpu_rope_f32_avx2_exec(const void *device_ctx, const marmot_kernel_args_rope_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    return marmot_cpu_rope_dispatch(device_ctx, args);
}
#endif

// rope_f32_accelerate: RoPE operation
#if MARMOT_ENABLE_ACCELERATE
marmot_error_t marmot_cpu_rope_f32_accelerate_exec(const void *device_ctx, const marmot_kernel_args_rope_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    return marmot_cpu_rope_dispatch(device_ctx, args);
}
#endif

// rope_bf16_accelerate: RoPE operation
#if MARMOT_ENABLE_ACCELERATE
marmot_error_t marmot_cpu_rope_bf16_accelerate_exec(const void *device_ctx, const marmot_kernel_args_rope_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    return marmot_cpu_rope_dispatch(device_ctx, args);
}
#endif

// rope_f64_neon: RoPE operation
#if MARMOT_ENABLE_NEON
marmot_error_t marmot_cpu_rope_f64_neon_exec(const void *device_ctx, const marmot_kernel_args_rope_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    return marmot_cpu_rope_dispatch(device_ctx, args);
}
#endif

// rope_f64_avx2: RoPE operation
#if MARMOT_ENABLE_AVX2
marmot_error_t marmot_cpu_rope_f64_avx2_exec(const void *device_ctx, const marmot_kernel_args_rope_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    return marmot_cpu_rope_dispatch(device_ctx, args);
}
#endif

// rope_f64_accelerate: RoPE operation
#if MARMOT_ENABLE_ACCELERATE
marmot_error_t marmot_cpu_rope_f64_accelerate_exec(const void *device_ctx, const marmot_kernel_args_rope_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    return marmot_cpu_rope_dispatch(device_ctx, args);
}
#endif
