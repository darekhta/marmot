// SPDX-License-Identifier: MIT
// This file was autogenerated by scripts/codegen/gen_api_dispatch.py; DO NOT EDIT.

#include "core/api/ops_internal.h"
#include "graph/kernel_dispatch_args.gen.h"

marmot_error_t marmot_reduce_sum(const marmot_context_t *ctx, const marmot_reduction_desc_t *desc) {
    if (ctx == nullptr || desc == nullptr || desc->input == nullptr || desc->out == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Reduce sum requires valid context and descriptor");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_reduction_params_t params = marmot_desc_to_params(desc);
    marmot_tensor_t *indices = false ? desc->indices_out : nullptr;
    return marmot_dispatch_reduction(ctx, MARMOT_DEVICE_REDUCTION_SUM, desc->input, desc->out, indices, &params, "sum");
}

marmot_error_t marmot_reduce_mean(const marmot_context_t *ctx, const marmot_reduction_desc_t *desc) {
    if (ctx == nullptr || desc == nullptr || desc->input == nullptr || desc->out == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Reduce mean requires valid context and descriptor");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_reduction_params_t params = marmot_desc_to_params(desc);
    marmot_tensor_t *indices = false ? desc->indices_out : nullptr;
    return marmot_dispatch_reduction(ctx, MARMOT_DEVICE_REDUCTION_MEAN, desc->input, desc->out, indices, &params, "mean");
}

marmot_error_t marmot_reduce_prod(const marmot_context_t *ctx, const marmot_reduction_desc_t *desc) {
    if (ctx == nullptr || desc == nullptr || desc->input == nullptr || desc->out == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Reduce prod requires valid context and descriptor");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_reduction_params_t params = marmot_desc_to_params(desc);
    marmot_tensor_t *indices = false ? desc->indices_out : nullptr;
    return marmot_dispatch_reduction(ctx, MARMOT_DEVICE_REDUCTION_PROD, desc->input, desc->out, indices, &params, "prod");
}

marmot_error_t marmot_reduce_max(const marmot_context_t *ctx, const marmot_reduction_desc_t *desc) {
    if (ctx == nullptr || desc == nullptr || desc->input == nullptr || desc->out == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Reduce max requires valid context and descriptor");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_reduction_params_t params = marmot_desc_to_params(desc);
    marmot_tensor_t *indices = false ? desc->indices_out : nullptr;
    return marmot_dispatch_reduction(ctx, MARMOT_DEVICE_REDUCTION_MAX, desc->input, desc->out, indices, &params, "max");
}

marmot_error_t marmot_reduce_min(const marmot_context_t *ctx, const marmot_reduction_desc_t *desc) {
    if (ctx == nullptr || desc == nullptr || desc->input == nullptr || desc->out == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Reduce min requires valid context and descriptor");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_reduction_params_t params = marmot_desc_to_params(desc);
    marmot_tensor_t *indices = false ? desc->indices_out : nullptr;
    return marmot_dispatch_reduction(ctx, MARMOT_DEVICE_REDUCTION_MIN, desc->input, desc->out, indices, &params, "min");
}

marmot_error_t marmot_reduce_argmax(const marmot_context_t *ctx, const marmot_reduction_desc_t *desc) {
    if (ctx == nullptr || desc == nullptr || desc->input == nullptr || desc->out == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Reduce argmax requires valid context and descriptor");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_reduction_params_t params = marmot_desc_to_params(desc);
    marmot_tensor_t *indices = true ? desc->indices_out : nullptr;
    return marmot_dispatch_reduction(ctx, MARMOT_DEVICE_REDUCTION_ARGMAX, desc->input, desc->out, indices, &params, "argmax");
}

marmot_error_t marmot_reduce_argmin(const marmot_context_t *ctx, const marmot_reduction_desc_t *desc) {
    if (ctx == nullptr || desc == nullptr || desc->input == nullptr || desc->out == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Reduce argmin requires valid context and descriptor");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_reduction_params_t params = marmot_desc_to_params(desc);
    marmot_tensor_t *indices = true ? desc->indices_out : nullptr;
    return marmot_dispatch_reduction(ctx, MARMOT_DEVICE_REDUCTION_ARGMIN, desc->input, desc->out, indices, &params, "argmin");
}

marmot_error_t marmot_reduce_any(const marmot_context_t *ctx, const marmot_reduction_desc_t *desc) {
    if (ctx == nullptr || desc == nullptr || desc->input == nullptr || desc->out == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Reduce any requires valid context and descriptor");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_reduction_params_t params = marmot_desc_to_params(desc);
    marmot_tensor_t *indices = false ? desc->indices_out : nullptr;
    return marmot_dispatch_reduction(ctx, MARMOT_DEVICE_REDUCTION_ANY, desc->input, desc->out, indices, &params, "any");
}

marmot_error_t marmot_reduce_all(const marmot_context_t *ctx, const marmot_reduction_desc_t *desc) {
    if (ctx == nullptr || desc == nullptr || desc->input == nullptr || desc->out == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Reduce all requires valid context and descriptor");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_reduction_params_t params = marmot_desc_to_params(desc);
    marmot_tensor_t *indices = false ? desc->indices_out : nullptr;
    return marmot_dispatch_reduction(ctx, MARMOT_DEVICE_REDUCTION_ALL, desc->input, desc->out, indices, &params, "all");
}

marmot_error_t marmot_reduce_variance(const marmot_context_t *ctx, const marmot_reduction_desc_t *desc) {
    if (ctx == nullptr || desc == nullptr || desc->input == nullptr || desc->out == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Reduce variance requires valid context and descriptor");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_reduction_params_t params = marmot_desc_to_params(desc);
    marmot_tensor_t *indices = false ? desc->indices_out : nullptr;
    return marmot_dispatch_reduction(ctx, MARMOT_DEVICE_REDUCTION_VARIANCE, desc->input, desc->out, indices, &params, "variance");
}

marmot_error_t marmot_reduce_std(const marmot_context_t *ctx, const marmot_reduction_desc_t *desc) {
    if (ctx == nullptr || desc == nullptr || desc->input == nullptr || desc->out == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Reduce std requires valid context and descriptor");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_reduction_params_t params = marmot_desc_to_params(desc);
    marmot_tensor_t *indices = false ? desc->indices_out : nullptr;
    return marmot_dispatch_reduction(ctx, MARMOT_DEVICE_REDUCTION_STD, desc->input, desc->out, indices, &params, "std");
}

marmot_error_t marmot_reduce_norm_l1(const marmot_context_t *ctx, const marmot_reduction_desc_t *desc) {
    if (ctx == nullptr || desc == nullptr || desc->input == nullptr || desc->out == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Reduce norm L1 requires valid context and descriptor");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_reduction_params_t params = marmot_desc_to_params(desc);
    marmot_tensor_t *indices = false ? desc->indices_out : nullptr;
    return marmot_dispatch_reduction(ctx, MARMOT_DEVICE_REDUCTION_NORM_L1, desc->input, desc->out, indices, &params, "norm L1");
}

marmot_error_t marmot_reduce_norm_l2(const marmot_context_t *ctx, const marmot_reduction_desc_t *desc) {
    if (ctx == nullptr || desc == nullptr || desc->input == nullptr || desc->out == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Reduce norm L2 requires valid context and descriptor");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_reduction_params_t params = marmot_desc_to_params(desc);
    marmot_tensor_t *indices = false ? desc->indices_out : nullptr;
    return marmot_dispatch_reduction(ctx, MARMOT_DEVICE_REDUCTION_NORM_L2, desc->input, desc->out, indices, &params, "norm L2");
}

