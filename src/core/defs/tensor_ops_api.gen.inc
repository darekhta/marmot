// SPDX-License-Identifier: MIT
// This file was autogenerated by scripts/codegen/gen_api_dispatch.py; DO NOT EDIT.

#include "core/api/ops_internal.h"
#include "graph/kernel_dispatch_args.gen.h"

marmot_error_t marmot_reshape(
    const marmot_context_t *ctx, const marmot_tensor_t *x, marmot_tensor_t *out, const size_t *new_shape,
    size_t new_ndim
) {
    if (ctx == nullptr || x == nullptr || out == nullptr || new_shape == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Reshape requires non-null context, tensors, and shape");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_op_signature_t sig = {0};
    marmot_kernel_args_reshape_t packed = {0};
    marmot_error_t build_status = marmot_reshape_build(ctx, x, out, new_shape, new_ndim, &sig, &packed);
    if (build_status != MARMOT_SUCCESS) {
        return build_status;
    }
    return marmot_execute_signature(ctx, &sig, &packed, "Reshape");
}

marmot_error_t marmot_view(const marmot_context_t *ctx, const marmot_tensor_t *x, marmot_tensor_t *out, size_t byte_offset) {
    if (ctx == nullptr || x == nullptr || out == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "View requires non-null context and tensors");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_op_signature_t sig = {0};
    marmot_kernel_args_view_t packed = {0};
    marmot_error_t build_status = marmot_view_build(ctx, x, out, byte_offset, &sig, &packed);
    if (build_status != MARMOT_SUCCESS) {
        return build_status;
    }
    return marmot_execute_signature(ctx, &sig, &packed, "View");
}

marmot_error_t marmot_contiguous(const marmot_context_t *ctx, const marmot_tensor_t *x, marmot_tensor_t *out) {
    if (ctx == nullptr || x == nullptr || out == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Contiguous requires non-null context and tensors");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_op_signature_t sig = {0};
    marmot_kernel_args_contiguous_t packed = {0};
    marmot_error_t build_status = marmot_contiguous_build(ctx, x, out, &sig, &packed);
    if (build_status != MARMOT_SUCCESS) {
        return build_status;
    }
    return marmot_execute_signature(ctx, &sig, &packed, "Contiguous");
}

marmot_error_t marmot_transpose(const marmot_context_t *ctx, const marmot_tensor_t *x, marmot_tensor_t *out, const int *perm) {
    if (ctx == nullptr || x == nullptr || out == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Transpose requires non-null context and tensors");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_op_signature_t sig = {0};
    marmot_kernel_args_transpose_t packed = {0};
    marmot_error_t build_status = marmot_transpose_build(ctx, x, out, perm, &sig, &packed);
    if (build_status != MARMOT_SUCCESS) {
        return build_status;
    }
    return marmot_execute_signature(ctx, &sig, &packed, "Transpose");
}

marmot_error_t marmot_concat(
    const marmot_context_t *ctx, const marmot_tensor_t *const *tensors, size_t num_tensors, marmot_tensor_t *out,
    int axis
) {
    if (ctx == nullptr || tensors == nullptr || out == nullptr || num_tensors == 0) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Concat requires non-null context, tensors, and outputs");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_op_signature_t sig = {0};
    marmot_kernel_args_concat_t packed = {0};
    marmot_error_t build_status = marmot_concat_build(ctx, tensors, num_tensors, out, axis, &sig, &packed);
    if (build_status != MARMOT_SUCCESS) {
        return build_status;
    }
    return marmot_execute_signature(ctx, &sig, &packed, "Concat");
}

marmot_error_t marmot_slice(
    const marmot_context_t *ctx, const marmot_tensor_t *x, marmot_tensor_t *out, const size_t *starts,
    const size_t *sizes
) {
    if (ctx == nullptr || x == nullptr || out == nullptr || starts == nullptr || sizes == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Slice requires non-null context, tensors, and slice metadata");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_op_signature_t sig = {0};
    marmot_kernel_args_slice_t packed = {0};
    marmot_error_t build_status = marmot_slice_build(ctx, x, out, starts, sizes, &sig, &packed);
    if (build_status != MARMOT_SUCCESS) {
        return build_status;
    }
    return marmot_execute_signature(ctx, &sig, &packed, "Slice");
}

marmot_error_t marmot_gather_rows(
    const marmot_context_t *ctx, const marmot_tensor_t *input, const marmot_tensor_t *indices,
    marmot_tensor_t *output
) {
    if (ctx == nullptr || input == nullptr || indices == nullptr || output == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Gather rows requires non-null context and tensors");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_op_signature_t sig = {0};
    marmot_kernel_args_gather_rows_t packed = {0};
    marmot_error_t build_status = marmot_gather_rows_build(ctx, input, indices, output, &sig, &packed);
    if (build_status != MARMOT_SUCCESS) {
        return build_status;
    }
    return marmot_execute_signature(ctx, &sig, &packed, "GatherRows");
}

marmot_error_t marmot_scatter_u64_to_i32(
    const marmot_context_t *ctx, const marmot_tensor_t *input, const marmot_tensor_t *indices,
    marmot_tensor_t *output
) {
    if (ctx == nullptr || input == nullptr || indices == nullptr || output == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Scatter u64 to i32 requires non-null context and tensors");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_op_signature_t sig = {0};
    marmot_kernel_args_gather_rows_t packed = {0};
    marmot_error_t build_status = marmot_scatter_u64_to_i32_build(ctx, input, indices, output, &sig, &packed);
    if (build_status != MARMOT_SUCCESS) {
        return build_status;
    }
    return marmot_execute_signature(ctx, &sig, &packed, "ScatterU64ToI32");
}

