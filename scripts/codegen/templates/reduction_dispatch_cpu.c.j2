// SPDX-License-Identifier: MIT
// This file was autogenerated by scripts/codegen/gen_kernels.py; DO NOT EDIT.
// Reduction dispatch: sum, mean, max, min, argmax, argmin, etc.

#include <stdlib.h>
#include <stdint.h>

#include "marmot/device.h"
#include "marmot/error.h"
#include "marmot/tensor.h"

#include "cpu_backend_internal.h"
#include "graph/kernel_dispatch_args.gen.h"

// ============================================================================
// Reduction Operation Implementations
// ============================================================================
{% for kernel in reduction_kernels %}
// {{ kernel.name }}: reduction op {{ kernel.op_enum }}
{% if kernel.platform_guard and kernel.platform_guard != "1" %}#if {{ kernel.platform_guard }}
{% endif %}
{% if kernel.requires_fp8 %}#if MARMOT_ENABLE_FP8
{% endif %}
marmot_error_t marmot_cpu_{{ kernel.name }}_exec(const void *device_ctx, const marmot_kernel_args_reduction_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
{% set helper_name = kernel.kernel_func if kernel.kernel_func else "cpu_" + kernel.name + "_impl" %}
    return {{ helper_name }}(
        device_ctx, args->input, args->out_values, args->out_indices, args->params
    );
}
{% if kernel.requires_fp8 %}#endif
{% endif %}
{% if kernel.platform_guard and kernel.platform_guard != "1" %}#endif
{% endif %}

{% endfor %}
