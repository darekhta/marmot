// SPDX-License-Identifier: MIT
// This file was autogenerated by scripts/codegen/gen_kernels.py; DO NOT EDIT.
// Elementwise dispatch: binary, unary, and ternary operations

#include <stdlib.h>
#include <stdint.h>

#include "marmot/device.h"
#include "marmot/error.h"
#include "marmot/tensor.h"

#include "cpu_backend_internal.h"
#include "graph/kernel_dispatch_args.gen.h"

// Forward declarations for direct elementwise kernels (generated from .def signatures).
{% for kernel in elementwise_kernels %}
{% if kernel.kind == 'binary' and kernel.kernel_func %}
{% if kernel.platform_guard and kernel.platform_guard != "1" %}#if {{ kernel.platform_guard }}
{% endif %}
{% if kernel.requires_fp8 %}#if MARMOT_ENABLE_FP8
{% endif %}
marmot_error_t {{ kernel.kernel_func }}(
    const void *device_ctx, const marmot_tensor_t *a, const marmot_tensor_t *b, marmot_tensor_t *out
);
{% if kernel.requires_fp8 %}#endif
{% endif %}
{% if kernel.platform_guard and kernel.platform_guard != "1" %}#endif
{% endif %}

{% endif %}
{% endfor %}

// Forward declarations for ternary kernels.
{% for kernel in elementwise_kernels %}
{% if kernel.kind == 'ternary' and kernel.kernel_func %}
{% if kernel.platform_guard and kernel.platform_guard != "1" %}#if {{ kernel.platform_guard }}
{% endif %}
{% if kernel.requires_fp8 %}#if MARMOT_ENABLE_FP8
{% endif %}
marmot_error_t {{ kernel.kernel_func }}(
    const void *device_ctx, const marmot_tensor_t *a, const marmot_tensor_t *b,
    const marmot_tensor_t *c, marmot_tensor_t *out
);
{% if kernel.requires_fp8 %}#endif
{% endif %}
{% if kernel.platform_guard and kernel.platform_guard != "1" %}#endif
{% endif %}

{% endif %}
{% endfor %}

// ============================================================================
// Binary Operation Implementations
// ============================================================================
{% for kernel in elementwise_kernels %}
{% if kernel.kind == 'binary' %}
// {{ kernel.name }}: binary op {{ kernel.op_enum }}
{% if kernel.platform_guard and kernel.platform_guard != "1" %}#if {{ kernel.platform_guard }}
{% endif %}
{% if kernel.requires_fp8 %}#if MARMOT_ENABLE_FP8
{% endif %}
marmot_error_t marmot_cpu_{{ kernel.name }}_exec(const void *device_ctx, const marmot_kernel_args_binary_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
{% if kernel.kernel_func %}
    return {{ kernel.kernel_func }}(device_ctx, args->input_a, args->input_b, args->output);
{% else %}
    return cpu_elementwise_binary_impl(device_ctx, {{ kernel.op_enum }}, args->input_a, args->input_b, args->output);
{% endif %}
}
{% if kernel.requires_fp8 %}#endif
{% endif %}
{% if kernel.platform_guard and kernel.platform_guard != "1" %}#endif
{% endif %}

{% endif %}
{% endfor %}

// ============================================================================
// Unary Operation Implementations
// ============================================================================
{% for kernel in elementwise_kernels %}
{% if kernel.kind == 'unary' %}
// {{ kernel.name }}: unary op {{ kernel.op_enum }}
{% if kernel.platform_guard and kernel.platform_guard != "1" %}#if {{ kernel.platform_guard }}
{% endif %}
marmot_error_t marmot_cpu_{{ kernel.name }}_exec(const void *device_ctx, const marmot_kernel_args_unary_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
{% if kernel.kernel_func %}
    return {{ kernel.kernel_func }}(device_ctx, {{ kernel.op_enum }}, args->input, args->params, args->output);
{% else %}
    return cpu_unary_apply(device_ctx, {{ kernel.op_enum }}, args->input, args->params, args->output);
{% endif %}
}
{% if kernel.platform_guard and kernel.platform_guard != "1" %}#endif
{% endif %}

{% endif %}
{% endfor %}

// ============================================================================
// Ternary Operation Implementations
// ============================================================================
{% for kernel in elementwise_kernels %}
{% if kernel.kind == 'ternary' and kernel.kernel_func %}
// {{ kernel.name }}: ternary op {{ kernel.op_enum }}
{% if kernel.platform_guard and kernel.platform_guard != "1" %}#if {{ kernel.platform_guard }}
{% endif %}
{% if kernel.requires_fp8 %}#if MARMOT_ENABLE_FP8
{% endif %}
marmot_error_t marmot_cpu_{{ kernel.name }}_exec(const void *device_ctx, const marmot_kernel_args_ternary_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    return {{ kernel.kernel_func }}(
        device_ctx, args->input_a, args->input_b, args->input_c, args->output
    );
}
{% if kernel.requires_fp8 %}#endif
{% endif %}
{% if kernel.platform_guard and kernel.platform_guard != "1" %}#endif
{% endif %}

{% endif %}
{% endfor %}
