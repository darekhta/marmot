// SPDX-License-Identifier: MIT
// This file was autogenerated by scripts/codegen/gen_kernels.py; DO NOT EDIT.
// Miscellaneous dispatch: quantization, conversion, embedding, tensor ops, vec_dot

#include <stdlib.h>
#include <stdint.h>

#include "marmot/device.h"
#include "marmot/error.h"
#include "marmot/tensor.h"

#include "cpu_backend_internal.h"
#include "graph/kernel_dispatch_args.gen.h"

// ============================================================================
// Quantization Implementations
// ============================================================================
{% for kernel in misc_kernels %}
{% if kernel.kind == 'quantization' %}
// {{ kernel.name }}: quantization op {{ kernel.op }}
{% if kernel.platform_guard and kernel.platform_guard != "1" %}#if {{ kernel.platform_guard }}
{% endif %}
{% if kernel.requires_fp8 %}#if MARMOT_ENABLE_FP8
{% endif %}
{% if kernel.op == 'quantize' %}
marmot_error_t marmot_cpu_{{ kernel.name }}_exec(const void *device_ctx, const marmot_kernel_args_quantize_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    const marmot_quant_kind_traits_t *traits = marmot_get_quant_kind_traits(args->kind);
    marmot_quant_layout_t layout = traits != nullptr ? args->layout : MARMOT_QUANT_LAYOUT_GENERIC;
    if (args->kind == MARMOT_QUANT_KIND_GENERIC) {
        return cpu_quantize(device_ctx, args->input, args->quant_params, args->output);
    }
    return cpu_quantize_with_kind(device_ctx, args->kind, layout, args->input, args->output);
}
{% elif kernel.op == 'dequantize' %}
marmot_error_t marmot_cpu_{{ kernel.name }}_exec(const void *device_ctx, const marmot_kernel_args_dequantize_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    const marmot_quant_kind_traits_t *traits = marmot_get_quant_kind_traits(args->kind);
    marmot_quant_layout_t layout = traits != nullptr ? args->layout : MARMOT_QUANT_LAYOUT_GENERIC;
    if (args->kind == MARMOT_QUANT_KIND_GENERIC) {
        return cpu_dequantize(device_ctx, args->input, args->output);
    }
    return cpu_dequantize_with_kind(device_ctx, args->kind, layout, args->input, args->output);
}
{% elif kernel.op == 'compute_quant_params' %}
marmot_error_t marmot_cpu_{{ kernel.name }}_exec(
    const void *device_ctx, const marmot_kernel_args_compute_qparams_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    return cpu_compute_quant_params(
        device_ctx, args->tensor, args->target_dtype, args->block_size, args->out_params
    );
}
{% endif %}
{% if kernel.requires_fp8 %}#endif
{% endif %}
{% if kernel.platform_guard and kernel.platform_guard != "1" %}#endif
{% endif %}

{% endif %}
{% endfor %}

// ============================================================================
// Conversion Implementations
// ============================================================================
{% for kernel in misc_kernels %}
{% if kernel.kind == 'conversion' %}
// {{ kernel.name }}: dtype conversion
{% if kernel.platform_guard and kernel.platform_guard != "1" %}#if {{ kernel.platform_guard }}
{% endif %}
{% if kernel.requires_fp8 %}#if MARMOT_ENABLE_FP8
{% endif %}
marmot_error_t marmot_cpu_{{ kernel.name }}_exec(const void *device_ctx, const marmot_kernel_args_convert_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    return cpu_convert_dispatch(
        device_ctx, args->dst_dtype, args->dst, args->src_dtype, args->src, args->n
    );
}
{% if kernel.requires_fp8 %}#endif
{% endif %}
{% if kernel.platform_guard and kernel.platform_guard != "1" %}#endif
{% endif %}

{% endif %}
{% endfor %}

// ============================================================================
// Embedding Implementations
// ============================================================================
{% for kernel in misc_kernels %}
{% if kernel.kind == 'embedding' %}
// {{ kernel.name }}: embedding gather
{% if kernel.platform_guard and kernel.platform_guard != "1" %}#if {{ kernel.platform_guard }}
{% endif %}
{% if kernel.requires_fp8 %}#if MARMOT_ENABLE_FP8
{% endif %}
marmot_error_t marmot_cpu_{{ kernel.name }}_exec(const void *device_ctx, const marmot_kernel_args_embedding_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    marmot_embedding_gather_desc_t desc = {
        .weights = args->weights,
        .token_ids = args->token_ids,
        .out = args->out,
        .dtype_out = args->dtype_out,
        .scale = args->scale,
        .padding_id = args->padding_id,
        .bounds_check = args->bounds_check,
        .prefer_gpu_private = args->prefer_gpu_private ? MARMOT_PREFERENCE_ENABLE : MARMOT_PREFERENCE_DISABLE,
        .allow_quant_decode_on_the_fly =
            args->allow_quant_decode_on_the_fly ? MARMOT_PREFERENCE_ENABLE : MARMOT_PREFERENCE_DISABLE,
    };
    return cpu_embedding_gather(device_ctx, &desc);
}
{% if kernel.requires_fp8 %}#endif
{% endif %}
{% if kernel.platform_guard and kernel.platform_guard != "1" %}#endif
{% endif %}

{% endif %}
{% endfor %}

// ============================================================================
// Vec Dot Implementations
// ============================================================================
{% for kernel in misc_kernels %}
{% if kernel.kind == 'vec_dot' %}
// {{ kernel.name }}: vector dot product
{% if kernel.platform_guard and kernel.platform_guard != "1" %}#if {{ kernel.platform_guard }}
{% endif %}
{% if kernel.requires_fp8 %}#if MARMOT_ENABLE_FP8
{% endif %}
marmot_error_t marmot_cpu_{{ kernel.name }}_exec(const void *device_ctx, const marmot_kernel_args_vec_dot_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    return cpu_vec_dot(device_ctx, args->desc, args->result);
}
{% if kernel.requires_fp8 %}#endif
{% endif %}
{% if kernel.platform_guard and kernel.platform_guard != "1" %}#endif
{% endif %}

{% endif %}
{% endfor %}

// ============================================================================
// Tensor Ops Implementations
// ============================================================================
{% for kernel in misc_kernels %}
{% if kernel.kind == 'tensor_ops' %}
// {{ kernel.name }}: tensor layout op
{% if kernel.platform_guard and kernel.platform_guard != "1" %}#if {{ kernel.platform_guard }}
{% endif %}
{% if kernel.requires_fp8 %}#if MARMOT_ENABLE_FP8
{% endif %}
{% if kernel.op == 'reshape' %}
marmot_error_t marmot_cpu_{{ kernel.name }}_exec(const void *device_ctx, const marmot_kernel_args_reshape_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    return cpu_reshape(device_ctx, args->input, args->output, args->new_shape, args->new_ndim);
}
{% elif kernel.op == 'view' %}
marmot_error_t marmot_cpu_{{ kernel.name }}_exec(const void *device_ctx, const marmot_kernel_args_view_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    return cpu_view(device_ctx, args->input, args->output, args->byte_offset);
}
{% elif kernel.op == 'contiguous' %}
marmot_error_t marmot_cpu_{{ kernel.name }}_exec(const void *device_ctx, const marmot_kernel_args_contiguous_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    return cpu_contiguous(device_ctx, args->input, args->output);
}
{% elif kernel.op == 'transpose' %}
marmot_error_t marmot_cpu_{{ kernel.name }}_exec(const void *device_ctx, const marmot_kernel_args_transpose_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    return cpu_transpose(device_ctx, args->input, args->output, args->perm);
}
{% elif kernel.op == 'concat' %}
marmot_error_t marmot_cpu_{{ kernel.name }}_exec(const void *device_ctx, const marmot_kernel_args_concat_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    return cpu_concat(device_ctx, args->inputs, args->num_inputs, args->output, args->axis);
}
{% elif kernel.op == 'slice' %}
marmot_error_t marmot_cpu_{{ kernel.name }}_exec(const void *device_ctx, const marmot_kernel_args_slice_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    return cpu_slice(device_ctx, args->input, args->output, args->starts, args->sizes);
}
{% elif kernel.op == 'gather_rows' %}
marmot_error_t marmot_cpu_{{ kernel.name }}_exec(
    const void *device_ctx, const marmot_kernel_args_gather_rows_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    return cpu_gather_rows(device_ctx, args->input, args->indices, args->output);
}
{% elif kernel.op == 'scatter_u64_to_i32' %}
marmot_error_t marmot_cpu_{{ kernel.name }}_exec(
    const void *device_ctx, const marmot_kernel_args_gather_rows_t *args) {
    if (args == nullptr) {
        marmot_set_error(MARMOT_ERROR_INVALID_ARGUMENT, "Kernel args not provided");
        return MARMOT_ERROR_INVALID_ARGUMENT;
    }
    return cpu_scatter_u64_to_i32(device_ctx, args->input, args->indices, args->output);
}
{% else %}
marmot_error_t marmot_cpu_{{ kernel.name }}_exec(const void *device_ctx, const void *args) {
    (void)device_ctx;
    (void)args;
    marmot_set_error(MARMOT_ERROR_NOT_IMPLEMENTED, "Tensor op {{ kernel.op }} not implemented for CPU");
    return MARMOT_ERROR_NOT_IMPLEMENTED;
}
{% endif %}
{% if kernel.requires_fp8 %}#endif
{% endif %}
{% if kernel.platform_guard and kernel.platform_guard != "1" %}#endif
{% endif %}

{% endif %}
{% endfor %}
